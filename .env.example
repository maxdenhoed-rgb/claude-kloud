# Step 1 needs no environment variables. Both lines below are placeholders for later steps.
# Uncomment when the corresponding service is wired up.

# Postgres connection string (step 2 onward).
# DATABASE_URL=postgres://user:pass@localhost:5432/claude_kloud
