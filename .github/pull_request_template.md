# What and why

<!-- One paragraph: the change, and why it lands now. -->

# Checklist

- [ ] `pnpm typecheck` passes locally.
- [ ] `pnpm lint` passes locally.
- [ ] `pnpm test` passes locally.
- [ ] `pnpm build` passes locally.
- [ ] No forbidden words from `design-artifacts/design-identity.md` ("amazing," "awesome," "exciting," "let's," "journey," "unlock," "level up," "boost," "supercharge," "AI-powered") in copy or commit messages.
- [ ] If a `REQ-` from `docs/requirements-engineering-cicd.md` is deviated from, the rationale is in this PR description.
- [ ] If the change is architecturally significant, a new ADR is added under `adrs/`.

# Linked issue

<!-- Encouraged while solo. Required once team size >= 2 (sunset condition in ADR-0006). -->

Closes #
